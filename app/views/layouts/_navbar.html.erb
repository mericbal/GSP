    <nav class="navbar navbar-inverse navbar-fixed-top bg-faded"> 
      <div class="container-fluid">
        <div class="navbar-header"> 
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
          <span class="icon-bar"> </span>
          <span class="icon-bar"> </span>
          <span class="icon-bar"> </span>                        
        </button>
          <%= link_to 'GSP', :root, class: 'navbar-brand' %>
        <% if current_user %>
        <!-- 
          <%= link_to 'log out', destroy_user_session_path, method: :delete, data: {confirm: ' log out ?'}, class: 'navbar-brand'%>
         -->
        <% end %>
        </div>
        
        <div class="collapse navbar-collapse" id="myNavbar">
          <ul class="nav navbar-nav navbar-right">
            <!-- <li><a href="#about">ABOUT</a></li> -->
          <% if !current_user %>
          <!-- 
            <li> <%= button_to 'Log in', new_user_session_path, class: 'btn-xs btn-primary navbar-btn' %> </li>
           -->
          <% else %>

            <li> <em class="navbar-brand" id="green"> <%= current_user.username %> </em> </li>
            <li> <em class="navbar-brand"> <%= link_to 'Edit me', edit_user_registration_path, class: 'btn btn-sm btn-warning' %> </em> </li>
            <li> <em class="navbar-brand"> <%= link_to 'Log out', destroy_user_session_path, method: :delete, data: {confirm: ' log out ?'}, class: 'btn btn-sm btn-danger'%> </em> </li>
          <% end %>
          </ul>
        </div>
      </div>
    </nav>