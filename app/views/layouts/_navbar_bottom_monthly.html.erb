<nav class="navbar navbar-fixed-bottom bg-faded"> 
	<div class="container">
		<p>
  		<%= link_to '<- Back', :back, class: 'btn btn-primary btn-sm' %>
  		<%= link_to 'Add a new monthly client', new_location_monthly_path(@location), class: 'btn btn-warning btn-lg' %> </p>
	</div>
</nav>